# Budget

Локальный PWA для учёта личных расходов. Без регистрации, backend и телеметрии — все данные лежат в IndexedDB на устройстве.

Стек: React 19, TypeScript (strict), Vite 8, Dexie, date-fns, Recharts, vite-plugin-pwa.

## Статус v0.3

Релиз надёжности: два бага v0.2 починены, данные, которые они могли испортить, восстанавливаются при первом же запуске.

| Что | Статус |
|---|---|
| Удаление счёта видит регулярные операции и переносит их вместе с историей | готово |
| Возобновление расписания не создаёт платежей за время паузы | готово |
| Миграция v3: потерянные операции и правила чинятся, ничего не удаляя | готово |
| Регулярные переводы между счетами | готово |
| Перевод внутри счёта читается и правится | готово |
| Экспорт копии и CSV через системное «Поделиться» | готово |
| Напоминание о резервной копии и дата последней копии в настройках | готово |
| Режим диагностики в production-сборке | готово |
| Сценарии Playwright в webkit и chromium | готово |

Версии: база — 3, формат резервной копии — 3. Базы v1 и v2 поднимаются при первом запуске, копии v0.1 и v0.2 импортируются как есть.

### Что чинит миграция v3

Баг v0.2 удалял счёт, не замечая, что на него ссылаются регулярные операции. Причина была не в логике, а в схеме: у таблицы `recurringTransactions` не было индексов по `accountId`, поэтому запрос по счёту просто не писался. На устройствах, где баг сработал, остались операции и правила на несуществующий счёт — исправленный код их бы не показал.

Миграция при открытии базы:
1. Переносит операции с битой ссылкой на счёт на «Восстановленный счёт» — он создаётся, только если есть что чинить.
2. Правила с битой ссылкой тоже переносит, но **выключает**: куда их направить, решает человек.
3. Битую категорию заменяет на «Другое» того же типа.
4. Ничего не удаляет и не трогает записи, с которыми всё в порядке; повторный прогон не меняет ни строки.

На главной появляется полоса с кнопкой «Открыть счёт». Она исчезает сама, когда «Восстановленный счёт» опустеет и его удалят обычным способом. Тем же кодом чинится восстановление из копии: копия могла быть снята с уже испорченной базы.

## Статус v0.2

| Что | Статус |
|---|---|
| Счета: список с остатками и числом операций, карточка счёта с оборотами, удаление с переносом | готово |
| Категории: свои категории, эмодзи, удаление с выбором замены | готово |
| Операции: расход, доход, перевод между счетами; повтор операции | готово |
| Главная: баланс, месячный бюджет, лимиты категорий, последние операции | готово |
| История: список по дням, поиск, фильтры в шторке | готово |
| Календарь расходов по дням | готово |
| Статистика: сравнение месяцев, график по дням, основные категории | готово |
| Бюджеты: общий месячный лимит и лимиты категорий | готово |
| Регулярные операции с защитой от дублей | готово |
| Backup / Restore / CSV, импорт копий v0.1 | готово |
| PWA: офлайн, обновление по подтверждению | готово |
| Liquid Glass: токены, три ступени материала, запасные варианты | готово |

Данные v0.1 переносятся автоматически: база поднимается с версии 1 до 2 при первом запуске, ни одна операция не теряется.

## Удаление счёта и категории

Операции никогда не удаляются каскадно.

**Счёт.** Если на счёте есть операции, удалить его можно только выбрав другой счёт для переноса.
1. Нет операций — удаление после подтверждения.
2. Есть операции — панель с их числом, выбором счёта и кнопкой «Перенести и удалить счёт».
3. Переписываются все три ссылки: `accountId` у расходов и доходов, `fromAccountId` и `toAccountId` у переводов.
4. Начальный остаток удаляемого счёта прибавляется к выбранному — общий баланс не меняется.
5. Всё идёт в одной транзакции Dexie с контрольной проверкой перед удалением; тест роняет её посередине и проверяет откат.
6. Перенос только на счёт в той же валюте. Единственный счёт удалить нельзя, даже пустой.
7. Перевод, обе стороны которого после переноса свелись к одному счёту, остаётся в истории: он даёт нулевой вклад в остаток, а терять операцию пользователя нельзя.

8. Регулярные операции считаются наравне с обычными: счёт, занятый расписанием, не удаляется молча. Панель называет обе цифры — «12 операций и 2 регулярных платежа», — и переносит их одной транзакцией.
9. Регулярный перевод, у которого после переноса обе стороны свелись к одному счёту, **выключается**: иначе он продолжил бы создавать бессмысленные операции. Тост называет его по имени.

**Категория.** Если на категорию ссылаются операции или регулярные платежи, при удалении нужно выбрать замену — битого `categoryId` в истории не остаётся. Лимиты удаляемой категории снимаются, а не складываются с лимитом цели.

Чтобы стереть всю историю — «Настройки → Данные → Удалить все данные».

## Команды

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # typecheck + production-сборка в dist/
npm run preview    # проверить production-сборку и service worker
npm run lint       # oxlint
npm test           # vitest + fake-indexeddb: 268 тестов на настоящей схеме Dexie
npm run e2e        # Playwright: webkit и chromium по production-сборке
```

Тестами закрыты: разбор и форматирование сумм, остатки счетов, логика переводов, бюджеты и лимиты категорий, сравнение месяцев, генерация регулярных операций и защита от дублей, перенос при удалении счёта и категории, ремонт битых ссылок, миграции базы v1 → v2 → v3 и импорт копий v0.1 и v0.2.

### Сценарии Playwright

`npm run e2e` поднимает production-сборку (`npm run build` + `vite preview`) и проходит по ней семь сценариев: расход, перевод, удаление счёта с регулярным платежом, пауза и возобновление расписания, круг «копия → сброс → восстановление» и режим диагностики.

- Первый запуск: `npx playwright install webkit chromium`. webkit — тот самый движок, в котором приложение живёт на iPhone.
- Только один браузер: `npm run e2e:chromium`.
- Если Chromium в системе уже есть (CI-образы), путь к нему передаётся через `CHROMIUM_PATH=/путь/к/chrome`.

## Проверка на iPhone

Service worker (офлайн-режим) работает только по HTTPS или на localhost.

- Быстро посмотреть интерфейс: `npm run dev -- --host` и открыть `http://<IP-компьютера>:5173` в Safari. Установить на экран «Домой» можно, но без офлайна.
- Полноценно как PWA: задеплоить `dist/` на любой статический хостинг с HTTPS (Cloudflare Pages, Netlify, GitHub Pages, Vercel) → Safari → «Поделиться» → «На экран Домой».
  - Для Netlify добавьте `public/_redirects` со строкой `/* /index.html 200`, чтобы прямые ссылки на экраны открывались при первом заходе.
  - Для деплоя в подпапку (например, GitHub Pages `/budget/`) укажите `base: '/budget/'` в `vite.config.ts` — роутер и манифест это учитывают.

### Vercel

`vercel.json` уже в репозитории: прямые ссылки на экраны отдают `index.html`, а `sw.js` и `manifest.json` не кешируются — иначе обновление приложения доходило бы до телефона с задержкой в сутки.

```bash
npx vercel login
npx vercel --prod     # сборку и каталог dist/ Vercel определяет сам
```

Через веб-интерфейс: New Project → выбрать репозиторий → framework Vite → Deploy. Переменные окружения не нужны: у приложения нет backend'а.

### Что проверить руками после обновления на v0.3

Эмулятор не показывает ни настоящей клавиатуры, ни настоящих кадров. Список — по порядку, на телефоне, где уже есть данные:

1. **Данные на месте.** Открыть приложение после обновления: баланс, число операций и список за месяц совпадают с тем, что было до обновления.
2. **Полоса «Найдены операции без счёта».** Если она появилась — открыть счёт, проверить, что операции в нём знакомые, перенести их на настоящий счёт и удалить «Восстановленный».
3. **Удаление счёта.** Завести счёт, повесить на него регулярный платёж, попробовать удалить: приложение должно назвать регулярный платёж в панели переноса и после переноса сохранить правило на новом счёте.
4. **Пауза расписания.** Отключить регулярную операцию, вернуться через день, включить: платежей задним числом быть не должно, а если приложение спросит про пропущенные — «Не создавать» оставляет историю нетронутой.
5. **Регулярный перевод.** Создать перевод на накопительный счёт и убедиться, что после его создания общий баланс не изменился.
6. **Копия через «Поделиться».** «Настройки → Сохранить копию» → системное окно → «Сохранить в файлы». Затем восстановить из этого файла и сверить число операций.
7. **Клавиатура.** В форме суммы: кнопка сохранения и шторки должны стоять над клавиатурой и не прыгать при тапах по «Наличные» и «Карта».
8. **Кадры.** Семь нажатий по строке с версией → диагностика. Полистать список операций и посмотреть на счётчик: если проседает — переключить размытие на «Слабое» и сравнить.
9. **Офлайн.** Включить авиарежим и открыть приложение с экрана «Домой»: всё должно работать, кроме обновления.
10. **Обновление.** После следующего деплоя приложение предлагает обновиться — подтвердить и проверить, что данные на месте.

## Тестовые данные

В режиме разработки в «Настройках» есть блок Dev: «Добавить тестовые данные» и «Сбросить базу». В production-сборку этот код не попадает.

## Архитектура

```
src/
  app/          App, роутер, layout'ы, навигация (History API)
  pages/        экраны; у Dashboard свой хук useDashboardData
  features/     бизнес-логика по доменам: accounts, categories, transactions,
                budgets, recurring, analytics, backup, settings, diagnostics
  components/   переиспользуемые UI-компоненты (CSS Modules)
  db/           Dexie: database, schema, migrations, repair, seed
  hooks/        общие хуки (useToday, useKeyboardInset)
  utils/        money, dates, id, plural, download, keepFocus
  types/        сущности
  styles/       токены темы, стекло и глобальные стили
  dev/          моки только для разработки
e2e/            сценарии Playwright
```

Слои: UI (`pages`, `components`) → логика (`features/*/calculations.ts` — чистые функции) → хранилище (`features/*/repository.ts` → `db/`). Компоненты не обращаются к Dexie напрямую.

### UX-компоненты

- `Dialog` — нативный `<dialog>`: фокус, Esc и top layer делает браузер. На нём построены `Sheet` (нижняя шторка) и подтверждения.
- `useConfirm()` вместо `window.confirm()`, `useToast()` вместо `alert()`.
- `--keyboard-inset` (из `visualViewport`) поднимает кнопку «Добавить», тосты и шторки над клавиатурой iOS. Переменная пишется не чаще раза в кадр и только при фокусе в поле ввода: во время анимации клавиатуры iOS присылает пачку событий с рассогласованными `height` и `offsetTop`, и без этого шторка прыгала по экрану. Поднимаются панели `transform`'ом с коротким переходом — без пересчёта раскладки на каждый кадр.
- Кнопки-переключатели (`ChipGroup`, `SegmentedControl`, выбор иконки категории) отменяют `mousedown`, чтобы фокус остался в поле ввода: иначе тап по «Наличные» закрывал клавиатуру и перестраивал экран.
- `primeKeyboard()` — по тапу на «+» клавиатура открывается сразу, хотя поле суммы появляется чуть позже (iOS разрешает `focus()` только в обработчике жеста).

### Ключевые решения

- **Деньги** — целые копейки (`12550` = 125,50 ₴). Парсинг, форматирование и арифметика только через `Money` из `utils/money.ts`.
- **Даты операций** — строки `yyyy-MM-dd` в локальной зоне: день покупки не «уезжает» при смене часового пояса, а выборка за месяц — простое сравнение строк.
- **Миграции** — `db/migrations.ts`: массив версий, новые только добавляются в конец. `DB_VERSION` вычисляется из последней.
- **ID** — UUID v4 с запасным вариантом для небезопасного контекста (`http://192.168…`), где `crypto.randomUUID` недоступен.
- **Роутер** — ~60 строк на History API вместо react-router (−30 КБ gzip для шести плоских экранов). Если понадобятся вложенные маршруты — меняются только `app/navigation.ts` и `app/router.tsx`.
- **Операция — размеченное объединение**: `EntryTransaction | TransferTransaction`. У перевода нет `categoryId` и `accountId`, у расхода нет счетов перевода — TypeScript не даст прочитать чужое поле, пока тип не сужен. Правила собраны в `features/transactions/model.ts`.
- **Удаление счёта и категории** — см. раздел выше. Перенос и удаление идут в одной транзакции Dexie; тесты роняют её посередине и проверяют откат.
- **Регулярная операция — тоже размеченное объединение**: `RecurringEntry | RecurringTransfer`. Правила v0.2 — уже готовые `RecurringEntry`, поэтому данные не мигрируют. Помощники — в `features/recurring/model.ts`.
- **Регулярные операции** считаются от `startDate`, а не от предыдущего вхождения: иначе «31 января» уехало бы в 28 февраля и дальше. Защита от дублей — уникальный составной индекс `[recurringId+occurrenceDate]`.
- **Включение расписания идёт только через `setActive`**. Пауза по умолчанию означает, что платежей за это время не было, поэтому при включении ближайшее вхождение пересчитывается от сегодняшнего дня. Прямой `db.recurringTransactions.update(..., { isActive: true })` запрещён: он не трогает `nextOccurrence`, и первый же запуск генерации создал бы платежи за всю паузу. Досоздать пропущенное можно только явно — приложение спрашивает, сколько их, и по умолчанию не создаёт.
- **Ремонт данных** (`db/repair.ts`) — общий код для миграции и восстановления из копии. Идемпотентный: на здоровой базе не меняет ни одной записи.
- **Напоминание о копии** — чистая `shouldRemindBackup(settings, transactionCount, now)`. Молчит, пока операций меньше 15, и после сохранения копии; «Позже» откладывает на неделю, а не отключает.
- **Экспорт через `navigator.share`** с запасным скачиванием: в PWA на iOS обычная ссылка со скачиванием часто открывает файл во вкладке, и копию некуда положить. Файл собирается в памяти устройства и сам никуда не уходит — куда его деть, решает человек в системном окне.
- **Диагностика в production** — семь нажатий по строке с версией. Кадры, оба вьюпорта, `--keyboard-inset`, состояние service worker и переключатель размытия. Грузится отдельным чанком, только когда её открыли.
- **Шрифты** — системные (SF Pro, для сумм SF Pro Rounded). Никаких внешних запросов, в том числе к Google Fonts.

### Liquid Glass

Материал описан один раз в `styles/glass.css` — токены и утилита `.glass`, плюс компонент-обёртка `components/GlassSurface`. Вручную по компонентам стекло не прописывается.

- **Три ступени плотности** (`data-glass="subtle | regular | prominent"`). Чем выше слой, тем плотнее материал — так между ними читается глубина. Мелкие контролы — `subtle`, нижняя навигация — `regular`, шторки и диалоги — `prominent`.
- **Стекло только для плавающих слоёв**: навигация, шторки, переключатель месяца. Карточки с цифрами остаются непрозрачными — сумма важнее эффекта (ТЗ §63).
- **Кнопка «+» сплошная**: полупрозрачная главная кнопка потеряла бы контраст. От стекла у неё только блик и мягкая тень.
- **Blur умеренный** (10/16/22px): на iPhone каждый лишний пиксель размытия — это кадры.
- **Отдельные значения для тем**: одна и та же RGBA в тёмной теме выглядит грязным серым пятном.
- **Два запасных варианта**: `@supports not (backdrop-filter)` и `prefers-reduced-transparency: reduce` заменяют стекло непрозрачной поверхностью. Приложение от размытия не зависит.
- **Фон не плоский**: два очень мягких пятна на весь экран (`.app-backdrop`) — иначе стекло не читается. Фон лежит на `html`, а не на `body`: фон body рисуется как фон обычного блока и спрятал бы слой с отрицательным `z-index`.
- Анимации короткие и мягкие; `prefers-reduced-motion` глушит их глобально.
